install:
	pip install -r requirements.txt  # Ensure you have a requirements.txt listing Flask and other dependencies

run:
	flask run --port 3000  # This will run the Flask server on http://localhost:3000
